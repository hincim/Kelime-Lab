import 'package:flutter/material.dart';

Widget SolutionPage(BuildContext context) {
  double screenWidth = MediaQuery.of(context).size.width;

  return Container(
    color: Colors.white,
    child: Center(
        child: Text("Alıştırma Yap Ekranı"),
    ),
  );
  }